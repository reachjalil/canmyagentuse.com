# Captures

No immutable web archive captures were produced by this research environment. Every ledger row uses `[archive-failed:not-captured-in-research-environment]`. A publication pipeline should capture the original URL, archive URL, retrieval timestamp and content hash.
