# SaaS depth batch 1 — 2026-09-04

- Content commit: `9cdd6faf5d81abb6b15314dda1e1fb74bca56f92`
- CI: https://github.com/reachjalil/canmyagentuse.com/actions/runs/33942355141
- Guides: Cloudflare, Gmail, Notion, Linear
- Source entries: 30 additions; 69 total across 13 product guides
- Publication status: deployed and verified
- Worker version: `de50ca5f-426e-4ef8-9223-d6661bfd928d`, independently read back at 100% traffic
- CI conclusion: success

## Findings

- Gmail: Google's official remote MCP Developer Preview is now established.
  Workspace enrollment, Cloud project/API enablement, OAuth and the documented
  draft/read/label tools are explicit. API send permission is separate from the
  preview's tool list. Added scope boundaries, test-consent expiry, project-dependent
  quota changes and revocation.
- Notion: the official ntn CLI is established. Hosted MCP and PAT user permissions
  are distinct from API connection page grants. Added administrative and plan controls,
  limits, credential removal and a suggested first read followed by an approved write.
- Linear: official GraphQL API support is established. Added key/OAuth/team permissions,
  app-actor developer preview boundaries, plan/rate/complexity limits and revocation.
- Cloudflare: added precise preview ownership handoff, token/OAuth boundaries,
  Workers pricing and API limits, first account inspection and credential cleanup.

The compact directory and all old collections remain. No researched product was
installed, connected, purchased, or exercised against an account. Proposed task
walkthroughs remain explicitly unexecuted.

## Validation

- Coverage report and catalog validation passed; historical harness coverage unchanged.
- Biome passed.
- Uncached check: 175 Astro files, zero errors, warnings or hints.
- Uncached tests: 124 passed (73 catalog, 51 web).
- Production build passed.
- Cloudflare source assertions independently reviewed; root cross-checked material
  Gmail MCP/quota, Notion CLI/PAT/tool-limit and Linear GraphQL evidence.
- Local compiled HTML/Markdown/JSON checks passed for all 13 products and all source
  entries, social cards, machine indexes, search and preserved routes.
- Additional checks verified the three changed interface statuses, Gmail's preview
  qualification, Cloudflare's claim timing, retained detail disclosures, and GraphQL search.
- Updated Gmail and Linear social PNGs were visually inspected and readable.
- Fresh interactive browser inspection was unavailable because macOS was locked.
  This content-only release uses the previously verified templates and scripts;
  rendered HTML and generated social-image checks passed. No new browser/layout
  test is claimed for this cycle.

## Production verification

Cache-busted HTTP checks passed for all 13 guide HTML/Markdown/JSON records and
all 69 source entries, all guide social cards, submit-app HTML/Markdown/social,
OpenAPI, llms indexes, sitemap, product search, sponsor/press/prompt/skill pages,
and preserved harness/feature/matrix routes. Targeted checks confirmed Gmail's
MCP preview qualification, Notion CLI, Linear API, Cloudflare's claim deadline,
retained detail disclosures and GraphQL search results. Source/representation
parity is verified; no new live provider-task result is claimed.

## Next research

Deepen Salesforce org provisioning/permissions, then add Vercel, Supabase and
Sentry in bounded batches. Revisit Gmail MCP preview eligibility and announced
billing transitions. Keep signup, connection, payment, and useful actions separate.
