{
  "name": "@canmyagentuse/internal",
  "version": "0.1.0",
  "private": true,
  "description": "Internal launch-operations dashboard for Can My Agent Use. Local only; never deployed.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@canmyagentuse/catalog": "workspace:*",
    "@fontsource/ibm-plex-mono": "catalog:",
    "@fontsource/ibm-plex-sans": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
